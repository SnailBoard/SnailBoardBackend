version: "3.3"

services:
  db:
    image: mysql:5.7
    restart: always
    ports:
    - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: snailboard
      MYSQL_USER: root
      MYSQL_PASSWORD: root