server.port=8081
# Database
spring.datasource.username=${POSTGRES_USER:snailboard}
spring.datasource.password=${POSTGRES_PASSWORD:GRib1111}
spring.datasource.url=${DB_URL:jdbc:postgresql://snailboard-db-server.postgres.database.azure.com:5432/snailboard}
spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jackson.serialization.fail-on-empty-beans=false
spring.flyway.enabled=true
spring.flyway.url=${DB_URL:jdbc:postgresql://localhost:5432/snailboard}
spring.flyway.user=${POSTGRES_PASSWORD:root}
spring.flyway.password=${POSTGRES_PASSWORD:root}
spring.flyway.validateOnMigrate=false
jwt.tokenExpiration=300000
jwt.refreshTokenExpiration=2592000000
jwt.secret=chbd
jwt.refreshSecret=iv82))
server.error.include-message=always
email.from = snailboardinc@gmail.com
email.password = snailboard123
spring.web.resources.static-locations=file:snailboard_images/